services:
  db:
    image: postgres:16
    environment:
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=yourpassword
        - POSTGRES_DB=mydb
    ports:
      - 5433:5432
    volumes:
      - pgData:/var/lib/postgresql/data
volumes:
  pgData:
